import React from "react";
import { HeaderOfSection } from "./Styles";

const TechStack = () => {
    let header = "<Tech Stack/>";
    return (
        <div>
            <HeaderOfSection>
                <h2>{header}</h2>
            </HeaderOfSection>
        </div>
    );
};

export default TechStack;
